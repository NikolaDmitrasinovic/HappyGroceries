# CI troubleshooting

This repo uses GitHub Actions checks that must pass before merging.

## Format check failed (C#)

If the **C# formatting** check fails, run:

```bash
dotnet format ./backbone.slnx