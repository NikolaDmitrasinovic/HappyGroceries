services:
  nelidb:
   container_name: nelidb
   environment:
    POSTGRES_USER: ${POSTGRES_USER}
    POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    POSTGRES_DB: ${POSTGRES_DB}
   restart: always
   ports:
    - "5432:5432"
   volumes:
    - postgres_nelidb:/var/lib/postgresql/