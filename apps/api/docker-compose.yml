services:
  nelidb:
   image: postgres
   
  seq:
   image: datalust/seq:latest
   container_name: neli-seq
   environment:
     - ACCEPT_EULA=Y
     - SEQ_FIRSTRUN_NOAUTHENTICATION=True
   ports:
     - "5341:80"

volumes:
 postgres_nelidb:
 seq_data:
